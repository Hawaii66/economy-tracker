{
  "name": "budget-core-test",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "budget-core": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
